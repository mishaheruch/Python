# Робота з текстовими даними: split, len, count, lower, upper, etc.

## Теорія

### Мета: навчитися працювати з текстовими даними.

### 1. Спробуй вивчити функції для роботи з текстовими даними [ось тут](https://docs.python.org/uk/3.13/library/stdtypes.html#string-methods).

## Завдання

У цій папці створи файл `password_validator.py` і напиши у ньому код, який буде приймати від користувача рядок символів будь-якої довжини і буде визначати, чи цей рядок є надійним паролем. Надійний пароль має відповідати таким вимогам: не містить пробілів, має довжину від 12 до 40 символів, містить хоча б одну велику літеру, містить хота б одну малу літеру, містить хоча б одну цифру, містить хоча б один із спеціальних символів із наступного переліку: `!@#`. Якщо пароль не відповідає вимогам, то програма має пояснити, у чому проблема (або проблеми, якщо їх кілька).

Наприклад: якщо користувач введе `ksa123`, то програма має повертати `Пароль недостатньо надійний: мінімальна довжина паролю 12 символів, має бути хоча б одна велика літера, має бути один із спеціальних символів !@#`.

_Підказка. Це завдання можна вирішити кількома способами. Один із них – це використовувати цикли, про які ти вже знаєш. Інший спосіб - використвувати регулярні вирази. Це складніше і ти поки ще це не вивчав, але якщо ти справишся циклами і тобі буде нічим зайнятися, то перепиши твій код на регулярні вирази._





День 4
  
str.capitalize()

робить перший символ ВЕЛИКИМ
усі інші символи — малими
a = "Straße"
b = "STRASSE"

print(a.casefold() == b.casefold())
# True
lower() → для простого тексту
casefold() → для надійного порівняння

s.count("an")
1️⃣ Починає зліва
2️⃣ Шукає точне співпадіння "an"
3️⃣ Якщо знайшов — додає 1 до лічильника
4️⃣ Перескакує на кінець знайденого підрядка
5️⃣ Продовжує далі
str.count(sub[, start[, end]])
encode() → str ➜ bytes
UTF-8 — стандарт
errors — що робити з помилками
b'...' — це байти, не рядо
str.endswith() 
'Python'.endswith('on')
True
'Python'.endswith('py')
False
start і end — обмеження перевірки
'Python is amazing'.endswith('is', 0, 9)
'Python is'
'Python is amazing'[0:9].endswith('is')
endswith() нічого не змінює
він лише перевіряє
повертає True або False
expandtabs():
знаходить у рядку табуляції \tзамінює їх пробілами
кількість пробілів залежить від позиції (колонки) та tabsize
str.find()
sub — що шукаємо
start — з якого індексу шукати (необов’язково)
end — до якого індексу шукати (необов’язково)
if "Py" in s:
    ...
str.format()
format() підставляє значення в рядок у місця {}.
"{1} expects the {0} Inquisition!".format("Spanish", "Nobody")
0 → "Spanish"
1 → "Nobody"
Nobody expects the Spanish Inquisition!
"{:.1f}".format(2.345)   # 2.3
"{:.3f}".format(2.345)   # 2.345
class Default(dict):
    def __missing__(self, key):
        return key
Що це означає:
якщо ключа немає
замість помилки
повертається назва ключа
str.isalnum()
True — якщо
усі символи в рядку — букви або цифри
і рядок не порожній
False — в усіх інших випадках
isalpha() → буква
isdecimal() → десяткова цифра
isdigit() → цифра
isnumeric() → числовий символ
"abc 123".isalnum()  # пробіл
"abc-123".isalnum()  # дефіс
"abc!".isalnum()     # !
"".isalnum()         # порожній рядок
str.isalpha()
isalpha() повертає:
True — якщо
усі символи — літери 
False — в усіх інших випадках
"LettersOnly".isalpha()
"Привіт".isalpha()
"µ".isalpha()
Добре 
"Letters and spaces".isalpha()  # пробіли
"Hello123".isalpha()            # цифри
"Hello!".isalpha()              # !
"".isalpha()                    # порожній рядок
не добре 
str.isascii() перевіряє:
True — якщо
рядок порожній, або
усі символи — ASCII
 False — якщо є хоч один не-ASCII символ

латинські літери A–Z, a–z
цифри 0–9
пробіл
базові знаки: ! @ # $ % тощо
керуючі символи (\n, \t)
ЛИШЕ  ЦІ МОЖНА ПИСАТИ 
isidentifier() → форма правильна
iskeyword() → чи зарезервоване
Для зміної потрібні обидві перевірки
str.islower()
True, якщо
усі літери — малі
і є хоча б одна літера
False — в інших випадках
str.isnumeric()
True — якщо
усі символи в рядку є числовими
і рядок не порожній
 False — інакше
Що означає “числові символи”?
Це всі символи, які Unicode вважає числами:
1️⃣ десяткові цифри 2️⃣ цифри інших систем письма 3️⃣ дроби, степені, римські цифри тощо

Правда
"0123456789".isnumeric()
"٠١٢٣٤٥٦٧٨٩".isnumeric()
"⅕".isnumeric()      # 1/5
"²".isnumeric()      # квадрат

Не правда
 "12.3".isnumeric()   # крапка
"-123".isnumeric()   # мінус
"abc".isnumeric()
"".isnumeric()
str.isprintable()
 True — якщо всі символи можна надрукувати
False — якщо є хоч один непечатний символ
Печатні
літери (будь-які Unicode)
цифри
розділові знаки
символи (@, #, €, emoji 😄)
звичайний пробіл
НЕпечатні
керуючі символи:
\n (перенос рядка)
\t (таб)
\r
інші службові Unicode-символи
str.isspace()
True — якщо
усі символи — пробільні
і рядок не порожній
False — в інших випадках
" ".isspace()
"\t".isspace()
"\n".isspace()
" \t\n".isspace()
Добре 
"".isspace()          # порожній рядок
" a ".isspace()       # є літера
"123".isspace()       # цифри
"  .  ".isspace()     # крапка — не пробіл
НІ
str.istitle()
True — якщо рядок оформлений як заголовок
False — інакше
"Spam, Spam, Spam".istitle()
"Hello World".istitle()
"Привіт Світ".istitle()
Правда 
"spam, spam, spam".istitle()   # всі малі
"SPAM, SPAM, SPAM".istitle()   # всі великі
"Hello world".istitle()        # world з малої
"Hello WORLD".istitle()        # WORLD не title-case
не правда 
str.isupper()
Всі цифри з великої 
'BANANA'.isupper()
True
'banana'.isupper()
False
'baNana'.isupper()
False
' '.isupper()
False
str.join(iterable)
', '.join(['spam', 'spam', 'spam'])
# 'spam, spam, spam'

'-'.join('Python')
# 'P-y-t-h-o-n'
str.lower() Повертає новий рядок, у якому всі літери перетворені на нижній регістр.
'PyThOn'.lower()
# 'python'

'HELLO world!'.lower()
# 'hello world!'
lstrip() видаляє символи З ПОЧАТКУ рядка.
Він не чіпає середину і кінець, тільки лівий бік
'   spacious   '.lstrip()
'spacious   '
'www.example.com'.lstrip('cmowz.')
'example.com'
str.maketrans() створює таблицю заміни символів, яку потім використовують з методом str.translate().
table = str.maketrans({'a': '1', 'b': '2'})
maketrans() → створює таблицю
translate() → застосовує їїможна замінювати і видаляти
дуже швидко працює з текстом
str.removesuffix(suffix, /)
'TestHook'.removeprefix('Test')

'BaseTestCase'.removeprefix('Test')
видаляє тільки початок
працює тільки якщо збіг
інакше повертає копію
доступний з Python 3.9
str.split()
розбиває рядок на частини
повертає список рядків
розбиття відбувається за роздільником sep
'1,2,3'.split(',')
['1', '2', '3']
'1,2,3'.split(',', maxsplit=1)
['1', '2,3']
split() → рядок ➜ список
з sep → строгий поділ
без sep → “розумний” поділ по пробілах
maxsplit обмежує кількість розбитті
зворотна операція → join(
str.startswith()
чи починається рядок з певного префікса
повертає True або False
рядок не змінюється
s.startswith(prefix, start, end)
prefix — рядок або кортеж рядків
start — з якого індексу перевіряти
end — до якого індексу перевіряти
"Python".startswith("Py")
Правда 
"Python".startswith("py")
Ні
str.strip()
видаляє символи з ПОЧАТКУ і КІНЦЯ рядка
середину рядка не чіпає
повертає новий рядок
str.swapcase()
міняє регістр кожної літери на протилежний
великі → малі
малі → великі
повертає новий рядок
str.translate()
проходиться по кожному символу рядка
дивиться в таблицю перекладу
замінює, видаляє або залишає символ
повертає новий рядок
table = str. maketrans("abc", "123")
"cab".translate(table)
312
upper() → всі літери ВЕЛИКІ
Unicode може давати винятки
zfill() → нулі зліва
знак + / - зберігається спереду
"42".zfill(5)

"-42".zfill(5)
